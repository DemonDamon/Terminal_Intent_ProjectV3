# 终端商机挖掘预测服务 - API 依赖
# Terminal Intent Prediction Service - API Requirements
# ======================================================
#
# 说明：此文件包含API服务运行所需的所有Python依赖
# 安装方式: pip install -r requirements-api.txt
#
# 依赖分类：
# 1. 核心ML依赖 (模型运行)
# 2. API框架依赖 (服务运行)
# 3. 数据处理依赖 (特征工程)
#
# ======================================================

# ============== 核心ML依赖 ==============
# LightGBM: 梯度提升模型框架
lightgbm>=4.1.0,<5.0.0

# Scikit-learn: ML工具库 (用于模型评估)
scikit-learn>=1.3.0,<2.0.0

# Joblib: 模型序列化
joblib>=1.3.0,<2.0.0


# ============== 数据处理依赖 ==============
# Pandas: 数据处理框架
pandas>=1.5.0,<3.0.0

# NumPy: 数值计算
numpy


# ============== API框架依赖 ==============
# FastAPI: 高性能API框架
fastapi>=0.100.0,<1.0.0

# Uvicorn: ASGI服务器
uvicorn[standard]>=0.23.0,<1.0.0

# Pydantic: 数据验证框架 (FastAPI依赖)
pydantic>=2.0.0,<3.0.0

# Pydantic-settings: 配置管理扩展
pydantic-settings>=2.0.0,<3.0.0

# Python-multipart: 表单数据处理
python-multipart>=0.0.6


# ============== 可选依赖 ==============
# tqdm: 进度条 (训练时使用)
tqdm>=4.65.0

# Optuna: 超参数优化 (训练时使用，API运行时不需要)
optuna>=3.4.0
